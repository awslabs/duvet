# Witness query dogfood — expected failures (LCOV-pending cross-crate pairs)
#
# Each line below is a correlation that CORRECTLY fails in a proof-only run:
# the implementation annotations live in the duvet crate (the engine), and
# the proof witnesses from duvet-coverage's SST logs structurally cannot
# reach them. Per Decision 8 (subset of producers), ALL implementations
# must be witnessed for discharge — so the pairs fail, and that is correct.
#
# These become passing when the LCOV mixed-coverage producer (decisions.md,
# Follow-ups § LCOV producer) supplies runtime witnesses that execute the
# engine code. At that point each line below becomes an unexpected pass and
# must be deleted (the exit criterion).
#
# Format: DUVET-AWAITING-LCOV <section> <file>:<line>
# The CI gate asserts the failed-correlation set is EXACTLY this list.
# A new failure or an unexpected pass both break the gate.

# W2 — engine discharge loop
DUVET-AWAITING-LCOV design/witness/spec.md#property-w2-test-execution duvet/src/query/engine.rs:714

# W6 — result struct and unwitnessed-test type
DUVET-AWAITING-LCOV design/witness/spec.md#property-w6-unwitnessed-test-annotations duvet/src/query/result.rs:77
DUVET-AWAITING-LCOV design/witness/spec.md#property-w6-unwitnessed-test-annotations duvet/src/query/result.rs:201

# W3 — engine global-execution fold + adapter wrapper
DUVET-AWAITING-LCOV design/witness/spec.md#property-w3-global-execution duvet/src/query/engine.rs:822
DUVET-AWAITING-LCOV design/witness/spec.md#property-w3-global-execution duvet/src/query/witness.rs:516
