SPECIFICATION: [Duvet Coverage Model: Formal Specification](design/query/coverage-model-spec.md)
  SECTION: [1.3 Classification Function and Unknown Lines](#classification-function)
    TEXT[!MUST]: A line classified as `{Annotation}` MUST NOT also have
    TEXT[!MUST]: `Statement` or `Declaration` in its property set.
    TEXT[!MUST]: Classifiers MUST apply a post-processing pass
    TEXT[!MUST]: after AST classification:
    TEXT[!MUST]: for any line that has `Annotation`, `Comment`, or `Whitespace`,
    TEXT[!MUST]: remove `Statement` and `Declaration` from its property set.

  SECTION: [1.5 Scopes](#scopes)
    TEXT[implementation,test]: A scope is a contiguous range of lines
    TEXT[implementation,test]: delimited by `ScopeOpen` and `ScopeClose` properties.
    TEXT[implementation,test]: Scopes nest.
    TEXT[!MUST]: **Scope Balance Contract:** {#scope-balance-contract}
    TEXT[!MUST]: A classifier MUST emit a balanced `ScopeOpen`/`ScopeClose` stream:
    TEXT[!MUST]: matching each `ScopeClose` against the most recent unmatched `ScopeOpen`,
    TEXT[!MUST]: no `ScopeClose` occurs with no open to match,
    TEXT[!MUST]: and no `ScopeOpen` is left unmatched at end of file.
    TEXT[!MUST,implementation]: When the stream is unbalanced,
    TEXT[!MUST,implementation]: the coverage model MUST NOT score annotations against the collapsed scope tree;
    TEXT[!MUST,implementation]: it MUST surface the file as a defeated classification and escalate
    TEXT[!MUST]: (see [Classifier Selection and Dispatch](#dispatch)).

  SECTION: [5. Correctness Properties](#correctness-properties)
    TEXT[!MUST,implementation,test]: These properties MUST be proven with Verus.
    TEXT[!MUST,implication]: The Verus proof files MUST carry duvet annotations
    TEXT[!MUST,implication]: linking each `proof fn` back to the corresponding property section
    TEXT[!MUST,implication]: in this document.

  SECTION: [Property 1: No False Positives](#property-1-no-false-positives)
    TEXT[!MUST,implication]: The implementation MUST prove that if
    TEXT[!MUST,implication]: `is_annotation_executed(annotation, ...) = Executed`,
    TEXT[!MUST,implication]: then there exists a line L such that:
    TEXT[implication]: - `coverage[L] == Hit`
    TEXT[implication]:   (L is directly reported as executed)
    TEXT[implication]: - L is in the same scope as the annotation's target
    TEXT[implication]: - Every line between L and the annotation's target (exclusive)
    TEXT[implication]:   is classified (`Some`)
    TEXT[implication]:   and has properties that are a subset of
    TEXT[implication]:   {Whitespace, Comment, Annotation, Declaration}
    TEXT[implication]: - No line between L and the annotation's target
    TEXT[implication]:   has the `ScopeClose` property
    TEXT[implication]: - No line between L and the annotation's target
    TEXT[implication]:   has the `ScopeOpen` property
    TEXT[implication]: - No line between L and the annotation's target
    TEXT[implication]:   is unknown (`None`)

  SECTION: [Property 2: No Cross-Scope Leakage](#property-2-no-cross-scope-leakage)
    TEXT[!MUST,implementation,implication,test]: The implementation MUST prove that
    TEXT[!MUST,implementation,implication,test]: for any two lines A and B
    TEXT[!MUST,implementation,implication,test]: where A is in scope S1 and B is in scope S2
    TEXT[!MUST,implementation,implication,test]: and S1 ≠ S2
    TEXT[!MUST,implementation,implication,test]: and S1 is not a parent of S2
    TEXT[!MUST,implementation,implication,test]: and S2 is not a parent of S1:

  SECTION: [Property 3: Conservative Fallback](#property-3-conservative-fallback)
    TEXT[!MUST,implementation,implication,test]: The implementation MUST prove that
    TEXT[!MUST,implementation,implication,test]: no backward propagation occurs WITHIN a scope
    TEXT[!MUST,implementation,implication,test]: that contains a `NonLinearControl` line.
    TEXT[!MAY,implication,test]: If an ancestor scope S contains `NonLinearControl`
    TEXT[!MAY,implication,test]: but a child scope S' does not,
    TEXT[!MAY,implication,test]: propagation MAY occur through S'.

  SECTION: [Property 4: Monotonicity](#property-4-monotonicity)
    TEXT[!MUST,implementation,implication,test]: The implementation MUST prove that
    TEXT[!MUST,implementation,implication,test]: given two coverage reports E1 and E2
    TEXT[!MUST,implementation,implication,test]: where E1 ⊆ E2
    TEXT[!MUST,implementation,implication,test]: (E2 reports all the same hits as E1, plus possibly more):

  SECTION: [Property 5: Annotation Stacking Transitivity](#property-5-stacking-transitivity)
    TEXT[!MUST,implementation,test]: The implementation MUST prove that
    TEXT[!MUST,implementation,implication,test]: if annotation A (lines a1..a2) is immediately above
    TEXT[!MUST,implementation,implication,test]: annotation B (lines b1..b2)
    TEXT[!MUST,implementation,implication,test]: with only whitespace, comments, or other annotations between them
    TEXT[!MUST,implementation,test]: ,
    TEXT[!MUST,implementation,test]: and `is_annotation_executed(B, ...) = Executed`,
    TEXT[!MUST,implementation,test]: then `is_annotation_executed(A, ...) = Executed`.

  SECTION: [Property 6: Unknown Safety](#property-6-unknown-safety)
    TEXT[!MUST,implementation,test]: The implementation MUST prove that
    TEXT[!MUST,implementation,test]: unknown lines cannot produce false positives.

  SECTION: [Property 7: Target Determinism](#property-7-target-determinism)
    TEXT[implication]: `annotation_target` is a pure function:
    TEXT[implication]: given the same annotation, classifications, and file length,
    TEXT[implication]: it always returns the same result.
    TEXT[implication]: This is free in Verus
    TEXT[implication]: (all `fn` in Verus are deterministic by construction).

  SECTION: [Property 8: Scope Well-formedness](#property-8-scope-well-formedness)
    TEXT[implication]: `build_scope_tree` produces a well-formed scope tree:
    TEXT[implication]: - Every scope has `open_line <= close_line`.
    TEXT[implication]: - If two scopes overlap,
    TEXT[implication]:   one strictly contains the other (proper nesting).
    TEXT[implication]:   No partial overlaps.

  SECTION: [Property 9: Execution Set Containment](#property-9-execution-set-containment)
    TEXT[implementation,test]: The execution set always contains all directly-hit lines.
    TEXT[implication]: If `coverage[line] == Hit`,
    TEXT[implication]: then `line ∈ execution_set(classifications, scopes, coverage)`.

  SECTION: [Property 10: Annotation Target Bounds](#property-10-annotation-target-bounds)
    TEXT[implication]: If `annotation_target(annotation, ...) = Some(target)`,
    TEXT[implication]: then `target.line_number > annotation.end_line`.

  SECTION: [Property 11: Scope-Stream Balance Detection](#property-11-scope-stream-balance-detection)
    TEXT[!MUST,implementation]: The implementation MUST prove that the balance detector returns balanced if and
    TEXT[!MUST,implementation]: only if the `ScopeOpen`/`ScopeClose` stream over the classified lines is balanced:
    TEXT[!MUST,implementation]: no `ScopeClose` occurs while the scope depth is zero, and the depth is zero at
    TEXT[!MUST,implementation]: end of file.
    TEXT[implementation,test]: A stream with no scope delimiters is balanced, and its whole-file scope is
    TEXT[implementation,test]: legitimate.

  SECTION: [7.4 Properties](#degraded-properties)
    TEXT[!MUST]: These properties MUST be proven with Verus for the degraded path,
    TEXT[!MUST]: alongside the Section 5 properties for the classified path.

  SECTION: [Property D1: Direct Observation](#property-d1-direct-observation)
    TEXT[!MUST,implication]: The implementation MUST prove that the degraded status is a direct observation
    TEXT[!MUST,implication]: of the target line's own coverage, never an inference propagated from another
    TEXT[!MUST,implication]: line.

  SECTION: [Property D2: Degraded Target Bounds](#property-d2-degraded-target-bounds)
    TEXT[!MUST,implication]: The implementation MUST prove that any `Executed` or `NotExecuted` degraded
    TEXT[!MUST,implication]: status resolves a target strictly below the annotation
    TEXT[!MUST,implication]: (`target > annotation.end_line`).

  SECTION: [Property D3: Degraded Stacking Transitivity](#property-d3-degraded-stacking)
    TEXT[!MUST,implication]: The implementation MUST prove that the degraded status depends on the annotation
    TEXT[!MUST,implication]: only through its resolved target: two annotations that resolve to the same
    TEXT[!MUST,implication]: target receive the same status.

  SECTION: [Property D4: Agreement with the Classified Model](#property-d4-agreement-with-classified)
    TEXT[!MUST,implication]: The implementation MUST prove that where the degraded path emits a verdict on a
    TEXT[!MUST,implication]: directly-hit line that a full classifier would mark as a plain `Statement` (and
    TEXT[!MUST,implication]: not `NonLinearControl`), it returns the same result the classified model
    TEXT[!MUST,implication]: `is_annotation_executed` returns, namely `Executed`.

  SECTION: [8.2 Trust Taxonomy and Response](#trust-taxonomy)
    TEXT[!MUST]: duvet MUST NOT silently substitute the coarse model or score against
    TEXT[!MUST]:   the collapsed scope tree; it MUST escalate, reporting each located issue.

  SECTION: [8.3 Escalation](#escalation)
    TEXT[!MUST]: When `report` consumes coverage, a defeated commitment MUST be a hard
    TEXT[!MUST]: error there, because `report` is the authoritative artifact.

SPECIFICATION: [Producer core: verified-model properties](design/witness/producer-core-spec.md)
  SECTION: [Producer core: verified-model properties](#producer-core-verified-model-properties)
    TEXT[!MUST]: [spec.md §5.3](spec.md#discharge-unit)–[§5.5](spec.md#verus-producer)
    TEXT[!MUST]: define what the Verus producer must do; this document states the
    TEXT[!MUST]: properties that MUST be proven with Verus, as a new phase of the
    TEXT[!MUST]: verified coverage model, over a verified model of the parsed
    TEXT[!MUST]: obligation graph.
    TEXT[!MUST]: The Verus proof files MUST carry duvet annotations citing the
    TEXT[!MUST]: anchors in this document.

  SECTION: [1. Model](#model)
    TEXT[!MUST]: -
    TEXT[!MUST,implementation]: **Edges** are the symbolic references that resolve to a block in
    TEXT[!MUST,implementation]:   the artifact; references to names with no block are not part of
    TEXT[!MUST,implementation]:   the obligation graph ([spec §5.4](spec.md#closure)) and MUST NOT
    TEXT[!MUST,implementation]:   appear as model edges.

  SECTION: [Property P1: Closure Fixpoint](#property-p1-closure-fixpoint)
    TEXT[!MUST,implementation,test]: The implementation MUST prove that the computed closure of a
    TEXT[!MUST,implementation,test]: discharge unit's root is exactly the downward-reachable set of the
    TEXT[!MUST,implementation,test]: obligation graph — the least fixpoint of the edge relation
    TEXT[!MUST,implementation,test]: containing the root:
    TEXT[implementation]: where `reachable` is the reflexive-transitive reach along reference
    TEXT[implementation]: edges.

  SECTION: [Property P2: Most-Specific-Wins Rooting](#property-p2-most-specific-wins)
    TEXT[!MUST,implementation,test]: The implementation MUST prove that the units selected for a
    TEXT[!MUST,implementation,test]: position are exactly the minimal-extent containing units at the
    TEXT[!MUST,implementation,test]: finest populated specificity level
    TEXT[!MUST]: ([spec §5.3](spec.md#discharge-unit),
    TEXT[!MUST]: decisions.md [Decision 19](decisions.md#decision-19)):
    TEXT[!MUST,implementation]: Consequences the proof MUST deliver: every selected unit contains
    TEXT[!MUST,implementation]: the position; no containing unit at a strictly finer specificity
    TEXT[!MUST,implementation]: level exists when an extent-level unit is selected; and ties at the
    TEXT[!MUST,implementation]: winning level and minimal extent are all selected — never chosen
    TEXT[!MUST,implementation]: among (decisions.md, [Decision 12](decisions.md#decision-12)).

  SECTION: [Property P3: Witness Assembly](#property-p3-witness-assembly)
    TEXT[!MUST,implementation,test]: The implementation MUST prove that a witness's line set equals the
    TEXT[!MUST,implementation,test]: union of the closure's per-file spans restricted to project files:

  SECTION: [Property P4: Filter Soundness](#property-p4-filter-soundness)
    TEXT[!MUST,implementation,test]: The implementation MUST prove that project filtering is a view,
    TEXT[!MUST,implementation,test]: not a truncation (decisions.md,
    TEXT[!MUST,implementation,test]: [Decision 7](decisions.md#decision-7)'s consulted semantics;
    TEXT[!MUST,implementation,test]: [spec §5.4](spec.md#closure)'s unfiltered traversal):
    TEXT[!MUST,implementation]: Edge traversal MUST NOT be filtered — `closure_reached` takes no
    TEXT[!MUST,implementation]: project view, so the reached set is project-independent by the
    TEXT[!MUST,implementation]: model's construction, and only the span projection is restricted.

SPECIFICATION: [Duvet Witness: Formal Specification](design/witness/spec.md)
  SECTION: [Duvet Witness: Formal Specification](#duvet-witness-formal-specification)
    TEXT[!MUST]: The requirement keywords MUST, MUST NOT, SHOULD, and MAY are to be
    TEXT[!MUST]: interpreted as described in RFC 2119.

  SECTION: [1.1 Test and implementation annotations](#annotations)
    TEXT[!MUST]: Placement note (degraded files): in a file with no language
    TEXT[!MUST]: classifier, resolution cannot recognize comment lines as skippable —
    TEXT[!MUST]: only blank lines and annotation lines are skipped — so an annotation
    TEXT[!MUST]: (stacked or not) MUST be the last comment block above the code it
    TEXT[!MUST]: targets; an intervening ordinary comment (e.g.

  SECTION: [1.2 Witness](#witness)
    TEXT[implementation,test]: A witness is the record of **one act of checking**:
    TEXT[implementation,test]: one test's execution, or one prover obligation's successful
    TEXT[implementation,test]: verification.
    TEXT[implementation,test]:     files:      Map<FilePath, CoverageReport>,
    TEXT[implementation,test]:                                    -- per file: line → CoverageStatus,
    TEXT[implementation,test]:                                    -- the existing verified type
    TEXT[!MUST]: A witness's `files` maps MUST be **closed**:
    TEXT[!MUST]: they contain every line the act touched,
    TEXT[!MUST]: under the producer's reachability relation
    TEXT[!MUST]: (execution for runtime producers,
    TEXT[!MUST]: obligation-graph closure for prover producers).

  SECTION: [1.3 Provenance](#provenance)
    TEXT[implementation,test]:     discharge_unit: Option<String>,
    TEXT[implementation,test]:                               -- prover producers only: the obligation
    TEXT[implementation,test]:                               -- the witness was constructed from
    TEXT[implementation,test]: `strength` records what kind of claim the witness supports:
    TEXT[implementation,test]: `Executed` (a runtime act ran these lines) or
    TEXT[implementation,test]: `Consulted` (a prover's elaboration reached these lines).
    TEXT[!MUST]: Verdict output MUST report every bound witness's label and
    TEXT[!MUST]: strength with its per-witness result ([§3](#verdict-output)), so a reader can judge
    TEXT[!MUST]: each claim
    TEXT[!MUST]: (decisions.md, [Decision 7](decisions.md#decision-7) and
    TEXT[!MUST]: [Decision 14](decisions.md#decision-14)).

  SECTION: [1.4 Executed](#executed)
    TEXT[implementation,test]: For an annotation X and witness w:
    TEXT[implementation,test]: ```
    TEXT[implementation,test]: executed(X, w)  ⟺  the coverage model scores X's resolved target
    TEXT[implementation,test]:                     Executed against w.files
    TEXT[implementation,test]: ```
    TEXT[implementation,test]: This is exactly the existing verified Phases 1–3
    TEXT[implementation,test]: (`is_annotation_executed`, or the degraded path),
    TEXT[implementation,test]: applied to one witness's coverage maps.
    TEXT[implementation,test]: This specification adds no new per-annotation scoring semantics.
    TEXT[implementation,test]: The verified Phase 4 layer implements the "or" per file: a
    TEXT[implementation,test]: `ScoringMode` routes each file to the classified or the degraded
    TEXT[implementation,test]: scorer, and engine trust-boundary refusals are encoded as
    TEXT[implementation,test]: `Unscorable` — binds nothing, executes nothing
    TEXT[implementation,test]: If w's `files` contains no map for X's file at all,
    TEXT[implementation,test]: `executed(X, w)` is false.

  SECTION: [1.5 Claim rules and binding](#claim-rules)
    TEXT[implementation,test]: A test annotation must find *its own* witness.
    TEXT[implementation,test]: Each witness carries one claim rule;
    TEXT[implementation,test]: `binds` is total over (annotation, witness) pairs:
    TEXT[implementation,test]: ClaimRule ::= ByExecution | ByRootSpan(file, line_range)
    TEXT[implementation,test]: binds(T, w)  ⟺  match w.claim:
    TEXT[implementation,test]:     ByExecution        → executed(T, w)
    TEXT[implementation,test]:     ByRootSpan(f, r)   → T's resolved target EXISTS and falls
    TEXT[implementation,test]:                           within r in file f
    TEXT[implementation,test]: An annotation with no resolved target (e.g.
    TEXT[!MUST,implementation,test]: a Structural
    TEXT[!MUST,implementation,test]: annotation) binds no ByRootSpan witness —
    TEXT[!MUST,implementation,test]: empty-target containment MUST NOT bind vacuously.
    TEXT[implementation,test]: `ByExecution` is the runtime rule:
    TEXT[implementation,test]: the report cannot record which test produced it,
    TEXT[implementation,test]: so the test claims the witness by evidence —
    TEXT[implementation,test]: its own lines are executed in it.
    TEXT[implementation,test]: This rule is sound only under witness individuation ([§4.2](#obligation-individuation)).
    TEXT[implementation,test]: `ByRootSpan` is the prover rule:
    TEXT[implementation,test]: the witness was constructed from the annotation's own position
    TEXT[implementation,test]: ([§5](#prover-producers)), so ownership is positional and holds by
    TEXT[implementation,test]: construction
    TEXT[!MUST,implementation,test]: If the engine's path-matching relation associates an annotation's
    TEXT[!MUST,implementation,test]: file with more than one witness file (or one witness file with
    TEXT[!MUST,implementation,test]: more than one source file), the engine MUST refuse the bind and
    TEXT[!MUST,implementation,test]: report the ambiguity rather than select — the same posture the
    TEXT[!MUST,implementation,test]: producer takes when translating positions into artifact
    TEXT[!MUST,implementation,test]: coordinates.

  SECTION: [1.6 Discharge](#discharge)
    TEXT[implementation,test]: ```
    TEXT[implementation,test]: witnesses_for(T)  =  { w ∈ delivered : binds(T, w) }
    TEXT[implementation,test]: discharged(T, I)  ⟺  witnesses_for(T) ≠ ∅
    TEXT[implementation,test]:                       ∧  ∀w ∈ witnesses_for(T) : executed(I, w)
    TEXT[implementation,test]: ```
    TEXT[implementation,test]: In words: a pair (T, I) is discharged when the test binds at
    TEXT[implementation,test]: least one witness and **every** witness it binds executed the
    TEXT[implementation,test]: implementation. Each bound witness individually must see both
    TEXT[implementation,test]: sides; one bound witness that never reaches the implementation is
    TEXT[implementation,test]: a vacuous claim and fails the pair

  SECTION: [1.7 Producer](#producer)
    TEXT[implementation,test]: A producer maps declared artifacts to witnesses:
    TEXT[implementation,test]: ```
    TEXT[implementation,test]: produce : (artifacts, annotations) → Vec<Witness>
    TEXT[implementation,test]: ```
    TEXT[!MAY,implementation,test]: Runtime producers MAY ignore the `annotations` argument
    TEXT[!MAY,implementation,test]: (their witnesses pre-exist in the artifact).
    TEXT[implementation,test]: Prover producers use it to construct witnesses
    TEXT[!MUST,implementation,test]: The producer-internal artifact format MUST NOT escape the
    TEXT[!MUST,implementation,test]: producer; the engine consumes only `Vec<Witness>`.
    TEXT[implementation,test]: The `annotations` argument is a **semantically inert
    TEXT[implementation,test]: optimization**, never a semantic input.
    TEXT[implementation,test]: The witness universe is defined by the artifact alone —
    TEXT[implementation,test]: conceptually one potential witness per obligation —
    TEXT[implementation,test]: and the argument only selects which members are materialized,
    TEXT[implementation,test]: so that producers need not close every obligation to serve a few
    TEXT[implementation,test]: annotations.
    TEXT[!MUST,implementation,test]: Normatively: every delivered witness MUST be a function of
    TEXT[!MUST,implementation,test]: (artifact, obligation) only — identical regardless of which
    TEXT[!MUST,implementation,test]: annotation caused its materialization, carrying no annotation
    TEXT[!MUST,implementation,test]: identity
    TEXT[!MUST,implementation,test]: — and the filtering MUST be sound:
    TEXT[!MUST,implementation,test]: for every requested annotation, binding and discharge verdicts
    TEXT[!MUST,implementation,test]: over the materialized set MUST equal the verdicts over the full
    TEXT[!MUST,implementation,test]: universe.

  SECTION: [2. Engine properties](#engine-properties)
    TEXT[!MUST]: These properties MUST be proven with Verus,
    TEXT[!MUST]: as a new phase of the verified coverage model
    TEXT[!MUST]: (the quantifier layer over the existing per-annotation cells).
    TEXT[!MUST]: The Verus proof files MUST carry duvet annotations citing the
    TEXT[!MUST]: anchors in this section.

  SECTION: [Property W1: Universal Same-Witness Discharge](#property-w1-same-witness-discharge)
    TEXT[!MUST,implementation,test]: The implementation MUST prove that it reports a pair (T, I)
    TEXT[!MUST,implementation,test]: discharged if and only if at least one delivered witness binds T
    TEXT[!MUST,implementation,test]: and every delivered witness that binds T executed I:
    TEXT[!MUST]: Evidence assembled from two different witnesses
    TEXT[!MUST]: (T bound by one, I executed by another) MUST NOT discharge;
    TEXT[!MUST]: a bound witness that did not execute I MUST fail the pair
    TEXT[!MUST]: (decisions.md, [Decision 14](decisions.md#decision-14)).

  SECTION: [Property W2: Test Execution](#property-w2-test-execution)
    TEXT[!MUST,implementation,test]: The implementation MUST prove that a test annotation is reported
    TEXT[!MUST,implementation,test]: executed if and only if some delivered witness binds it:
    TEXT[test]: ```
    TEXT[implementation,test]: report_test_executed(T, witnesses) = true
    TEXT[implementation,test]:     ⟺  ∃ w ∈ witnesses : binds(T, w)
    TEXT[test]: ```

  SECTION: [Property W3: Global Execution](#property-w3-global-execution)
    TEXT[!MUST,implementation,test]: The implementation MUST prove that an implementation annotation is
    TEXT[!MUST,implementation,test]: reported ever-executed if and only if some delivered witness
    TEXT[!MUST,implementation,test]: executed it:
    TEXT[test]: ```
    TEXT[test]: report_ever_executed(I, witnesses) = true
    TEXT[test]:     ⟺  ∃ w ∈ witnesses : executed(I, w)
    TEXT[test]: ```
    TEXT[!MUST,test]: This is a global property requiring no correlation;
    TEXT[!MUST,test]: it is deliberately weaker than [W1](#property-w1-same-witness-discharge)
    TEXT[!MUST,test]: and MUST NOT be used to
    TEXT[!MUST,test]: discharge pairs.

  SECTION: [Property W4: Failure Monotonicity](#property-w4-monotonicity)
    TEXT[!MUST,implementation,test]: The implementation MUST prove that adding a witness never flips a
    TEXT[!MUST,implementation,test]: failing pair to passing:
    TEXT[test]: ```
    TEXT[test]: witnesses ⊆ witnesses'  ⟹
    TEXT[test]:     (report_discharged(T, I, witnesses')
    TEXT[test]:         ⟹ report_discharged(T, I, witnesses)
    TEXT[test]:            ∨ ¬∃ w ∈ witnesses : binds(T, w))
    TEXT[test]: ```
    TEXT[!MAY,test]: Adding a witness MAY newly fail a previously-discharged pair —
    TEXT[!MAY,test]: that is deliberate: the added witness is a claim T now makes, and
    TEXT[!MAY,test]: if it does not reach I it is the vacuity being caught
    TEXT[!MAY,test]: (decisions.md, [Decision 14](decisions.md#decision-14)).
    TEXT[test]: The only way adding witnesses turns a non-discharged pair into a
    TEXT[test]: discharged one is by witnessing a previously *unwitnessed* test
    TEXT[test]: (the `witnesses_for(T) = ∅` case), never by outvoting a bound
    TEXT[test]: witness that failed.

  SECTION: [Property W5: Claim Refinement](#property-w5-claim-refinement)
    TEXT[!MUST,implementation,test]: The implementation MUST prove that binding under `ByExecution`
    TEXT[!MUST,implementation,test]: implies execution of the test in the same witness:

  SECTION: [Property W6: Unwitnessed Test Annotations Are Failures](#property-w6-unwitnessed-test-annotations)
    TEXT[!MUST,implementation,test]: The engine MUST report every test annotation for which no
    TEXT[!MUST,implementation,test]: delivered witness binds it —
    TEXT[!MUST,implementation,test]: across ALL configured producers —
    TEXT[!MUST,implementation,test]: as a failure, never silently:
    TEXT[test]: ```
    TEXT[implementation,test]: ¬∃ w ∈ witnesses : binds(T, w)   ⟹   T is reported unwitnessed
    TEXT[test]: ```
    TEXT[!MAY]: Consequence (intended): running a subset of producers MAY fail a
    TEXT[!MAY]: test annotation that the full set passes;
    TEXT[!MAY]: that behavior is correct
    TEXT[!MAY]: (decisions.md, [Decision 8](decisions.md#decision-8)).

  SECTION: [Property W7: Positional Binding is Map-Independent](#property-w7-positional-binding-map-independence)
    TEXT[!MUST,implementation,test]: The implementation MUST prove that binding under `ByRootSpan` does
    TEXT[!MUST,implementation,test]: not depend on the witness's coverage maps:
    TEXT[test]: ```
    TEXT[test]: w.claim = w'.claim = ByRootSpan(f, r)
    TEXT[test]:     ⟹  (binds(T, w) ⟺ binds(T, w'))
    TEXT[test]: ```
    TEXT[test]: Which annotations a positional witness binds is a function of its
    TEXT[test]: root span alone. Consequences: enlarging a proof witness's closure
    TEXT[test]: never extends the set of test annotations it witnesses, and a
    TEXT[test]: witness whose maps Hit-cover another test annotation's lines still
    TEXT[test]: does not witness it — one proof's witness cannot capture another
    TEXT[test]: proof's test annotation, however deep the dependency chain between
    TEXT[test]: the proofs. This is [W5](#property-w5-claim-refinement)'s
    TEXT[test]: complement: `ByExecution` binding is exactly map evidence;
    TEXT[test]: `ByRootSpan` binding is exactly geometry.

  SECTION: [3. Verdict output requirements](#verdict-output)
    TEXT[!MUST,implementation,test]: For every discharged pair, the output MUST name, in verbose
    TEXT[!MUST,implementation,test]: output, every bound witness (its label) and its strength ([§1.3](#provenance)).
    TEXT[!MUST,implementation,test]: For every pair that fails because a bound witness did not execute
    TEXT[!MUST,implementation,test]: the implementation (W1's universal clause), the output MUST list
    TEXT[!MUST,implementation,test]: **every** bound witness with its per-witness result
    TEXT[!MUST,implementation,test]: (executed I / did not execute I) and strength,
    TEXT[!MUST,implementation,test]: so the failing claim is identifiable —
    TEXT[!MUST,implementation,test]: the engine computes all of these to evaluate the verdict,
    TEXT[!MUST,implementation,test]: and the disagreement MUST never be silent
    TEXT[!MUST,implementation,test]: (decisions.md, [Decision 14](decisions.md#decision-14);
    TEXT[!MUST,implementation,test]: legibility improvements are tracked in
    TEXT[!MUST,implementation,test]: [Follow-ups](decisions.md#follow-ups) and never weaken the verdict).
    TEXT[!MUST,implementation,test]: For every unwitnessed test annotation (W6), the output MUST
    TEXT[!MUST,implementation,test]: identify the annotation and state that no configured producer
    TEXT[!MUST,implementation,test]: yielded a witness for it.

  SECTION: [4. Producer obligations and trusted base](#producer-obligations)
    TEXT[!MUST,implementation,test]: Where an obligation cannot be proven, it is a **named axiom** of
    TEXT[!MUST,implementation,test]: the trusted base and MUST be recorded as such.
    TEXT[implementation,test]: **producers deliver facts and never render verdicts.**
    TEXT[implementation,test]: Delivering zero witnesses for an annotation is a fact
    TEXT[implementation,test]: (possibly with a reason attached, [§5.2](#two-pass-construction)'s not-proof-testable),
    TEXT[implementation,test]: not a failure

  SECTION: [4.1 Closedness](#obligation-closedness)
    TEXT[!MUST,implementation,test]: Every delivered `files` map MUST be closed under the producer's
    TEXT[!MUST,implementation,test]: reachability relation ([§1.2](#witness)).
    TEXT[!SHOULD,implementation,test]: - Prover producers: closedness splits into
    TEXT[!SHOULD,implementation,test]:   (a) the verifier's record faithfully reflects what elaboration
    TEXT[!SHOULD,implementation,test]:   consulted — **axiom**, same category as trusting the verifier
    TEXT[!SHOULD,implementation,test]:   itself — and
    TEXT[!SHOULD,implementation,test]:   (b) the closure computation over that record is correct —
    TEXT[!SHOULD,implementation,test]:   our code, which SHOULD be verified in `duvet-coverage`
    TEXT[!SHOULD,implementation,test]:   (it is a pure graph fixpoint).

  SECTION: [4.2 Individuation](#obligation-individuation)
    TEXT[!MUST,implementation,test]: Every delivered witness MUST be the record of exactly one act of
    TEXT[!MUST,implementation,test]: checking.
    TEXT[implementation,test]: Runtime producers: individuation is the operator's
    TEXT[implementation,test]:   responsibility — one instrumented run per test.
    TEXT[implementation,test]:   The artifact does not record how it was produced,
    TEXT[implementation,test]:   so duvet does not attempt detection

  SECTION: [4.3 Producer testing](#obligation-testing)
    TEXT[!MUST]: Each producer MUST be unit-tested against golden artifacts
    TEXT[!MUST]: (a real report file; a real prover log),
    TEXT[!MUST]: since producers are unverified glue at the trust boundary.

  SECTION: [4.4 Engine glue](#engine-glue)
    TEXT[implementation,test]: The verified layer's guarantees ([§2](#engine-properties)) reach the user only through
    TEXT[implementation,test]: unverified engine glue. Each glue component is named, bounded,
    TEXT[implementation,test]: and unit-tested (the posture [§4.3](#obligation-testing) takes for producers):
    TEXT[!MUST]: - **G1 (file identity).** The engine adapter MUST map file paths
    TEXT[!MUST]:   to the model's file identities injectively and consistently
    TEXT[!MUST]:   across all annotations and witnesses in one run, and MUST
    TEXT[!MUST]:   deliver each witness's per-file maps free of duplicate
    TEXT[!MUST]:   identities.
    TEXT[!MUST,implementation,test]: - **G2 (call obligation).** The engine MUST compute every pair,
    TEXT[!MUST,implementation,test]:   test, and global verdict (Properties
    TEXT[!MUST,implementation,test]:   [W1](#property-w1-same-witness-discharge)–[W4](#property-w4-monotonicity),
    TEXT[!MUST,implementation,test]:   [W6](#property-w6-unwitnessed-test-annotations)) by calling the
    TEXT[!MUST,implementation,test]:   verified layer's functions, and MUST derive per-witness
    TEXT[!MUST,implementation,test]:   failure diagnostics ([§3](#verdict-output)) from the same verified cells;
    TEXT[!MUST,implementation,test]:   no parallel engine-side verdict computation may exist.
    TEXT[!MUST]: - **G3 (mode routing).** The adapter MUST assign each
    TEXT[!MUST]:   annotation's file the scoring mode its classification actually
    TEXT[!MUST]:   selected — classified, degraded, or the trust-boundary refusal
    TEXT[!MUST]:   that binds nothing and executes nothing
    TEXT[!MUST]:   ([§1.4](#executed); decisions.md, [Decision 15](decisions.md#decision-15)).
    TEXT[!MUST,implementation,test]: The adapter MUST establish
    TEXT[!MUST,implementation,test]:   the verified functions' preconditions at the boundary —
    TEXT[!MUST,implementation,test]:   filter or degrade before calling, never assume.
    TEXT[!MAY,implementation,test]: - **G4 (bound-set precomputation).** The glue MAY compute a test's
    TEXT[!MAY,implementation,test]:   bound-witness set once — each membership decided by the verified
    TEXT[!MAY,implementation,test]:   binding cell — and pass it to a bound-set-taking verified
    TEXT[!MAY,implementation,test]:   discharge entry point, provided the verified layer proves that
    TEXT[!MAY,implementation,test]:   entry point's verdict equal to [§1.6](#discharge) discharge over
    TEXT[!MAY,implementation,test]:   the full delivered set whenever the supplied set is exactly
    TEXT[!MAY,implementation,test]:   `witnesses_for(T)`, sound and complete by index.
    TEXT[!MUST,implementation,test]:   The exactness precondition MUST be established the G3 way:
    TEXT[!MUST,implementation,test]:   the set is assembled from the verified binding cells' results
    TEXT[!MUST,implementation,test]:   for the same test context and witness list, never recomputed
    TEXT[!MUST,implementation,test]:   engine-side.

  SECTION: [5. Prover producers](#prover-producers)
    TEXT[!MUST]: Every prover producer owes its artifact an investigation before
    TEXT[!MUST]: it ships: the discharge-unit and closure granularity the artifact
    TEXT[!MUST]: demonstrably records — demonstrated by inspection of real
    TEXT[!MUST]: artifacts, not assumed — MUST be established and consumed as that
    TEXT[!MUST]: producer's shipped floor
    TEXT[!MUST]: (decisions.md, [Decision 17](decisions.md#decision-17)).

  SECTION: [5.1 Witnessable annotations](#witnessable-annotations)
    TEXT[!MUST]: Prover producers MUST construct witnesses for `type=test`
    TEXT[!MUST]: annotations only
    TEXT[!MUST]: (decisions.md, [Decision 6](decisions.md#decision-6);
    TEXT[!MUST]: self-discharging implication annotations are a deferred separate
    TEXT[!MUST]: feature).

  SECTION: [5.2 Construction from annotations](#two-pass-construction)
    TEXT[implementation,test]: Prover witnesses are constructed, not found:
    TEXT[!MUST,implementation,test]: the source path matches
    TEXT[!MUST,implementation,test]: several artifact paths), it MUST abort the run with the ambiguity
    TEXT[!MUST,implementation,test]: rather than skip the position: silently dropping a position would
    TEXT[!MUST,implementation,test]: convert a configuration defect into a missing-witness verdict.
    TEXT[!MUST,implementation,test]: If a live annotation's resolved target is not proof-testable,
    TEXT[!MUST,implementation,test]: the producer MUST deliver no witness for it,
    TEXT[!MUST,implementation,test]: and the report MUST identify the annotation as
    TEXT[!MUST,implementation,test]: *not proof-testable* ("this position carries no dischargeable
    TEXT[!MUST,implementation,test]: obligation; it can only be witnessed by an execution-style
    TEXT[!MUST,implementation,test]: producer") — a report distinct from Property W6's
    TEXT[!MUST,implementation,test]: "no witness from any configured producer."
    TEXT[!MUST,implementation,test]: In a mixed run such an annotation binds runtime witnesses
    TEXT[!MUST,implementation,test]: normally.

  SECTION: [5.3 Discharge unit](#discharge-unit)
    TEXT[!MUST,implementation,test]: Its domain (`dom(du)`) MUST contain only positions where an
    TEXT[!MUST,implementation,test]: obligation is *rooted* — proof-element positions,
    TEXT[!MUST,implementation,test]: at every granularity the artifact demonstrably records
    TEXT[!MUST,implementation,test]: (decisions.md, Decisions 13, 17, 18):
    TEXT[!MUST,implementation,test]: fn/lemma headers (obligation extents), ensures clauses,
    TEXT[!MUST,implementation,test]: loop invariants, and proof asserts.
    TEXT[!MUST,implementation,test]: Executable body lines MUST NOT be in the domain:
    TEXT[!MUST,implementation,test]: no obligation is rooted at a body line —
    TEXT[!MUST,implementation,test]: body lines are material a proof consults,
    TEXT[!MUST,implementation,test]: not claims a prover discharges —
    TEXT[!MUST,implementation,test]: so a test annotation there is category-mismatched,
    TEXT[!MUST,implementation,test]: and is reported *not proof-testable* rather than unwitnessed
    TEXT[!MUST,implementation,test]: (decisions.md, [Decision 13](decisions.md#decision-13)).
    TEXT[implementation,test]: **Rooting is most-specific-wins** (decisions.md, Decision 19):
    TEXT[implementation,test]: an annotation roots the finest unit whose span contains its
    TEXT[implementation,test]: resolved position; the enclosing extent is the fallback for
    TEXT[implementation,test]: positions inside no finer unit.
    TEXT[!MUST,implementation,test]: A producer MUST NOT hoist an
    TEXT[!MUST,implementation,test]: annotation placed on a clause, invariant, or assert to the
    TEXT[!MUST,implementation,test]: enclosing function's unit.
    TEXT[!MAY,implementation,test]: Attribution MAY be ambiguous *within one specificity level*:
    TEXT[!MAY,implementation,test]: provers stamp generated obligations with the source range of the
    TEXT[!MAY,implementation,test]: declaration they were generated from,
    TEXT[!MAY,implementation,test]: so one proof-testable position can root several obligations at
    TEXT[!MAY,implementation,test]: the same level.
    TEXT[!MUST,implementation,test]: When N obligations root a position at the chosen level, the
    TEXT[!MUST,implementation,test]: producer MUST deliver one witness per rooting obligation and
    TEXT[!MUST,implementation,test]: MUST NOT select among them (decisions.md, Decision 12).

  SECTION: [5.4 Closure](#closure)
    TEXT[!MUST,implementation,test]: A constructed witness's `files` maps MUST equal the source spans
    TEXT[!MUST,implementation,test]: of the downward reachable set of the prover's obligation graph,
    TEXT[!MUST,implementation,test]: starting from the discharge unit.
    TEXT[implementation,test]: Reachability is transitive: the closure follows the obligation
    TEXT[implementation,test]: graph's reference edges through any number of call or reference
    TEXT[implementation,test]: hops — a lemma reaching a fn reaching a fn reaching a fn: all of
    TEXT[implementation,test]: them enter — until a fixpoint.
    TEXT[!MUST,implementation,test]: The closure MUST be **reflexive**: it includes the discharge
    TEXT[!MUST,implementation,test]: unit's own root span (`root ∈ closure(root)`), so that a witness
    TEXT[!MUST,implementation,test]: always scores its own annotation as executed and `ByRootSpan`
    TEXT[!MUST,implementation,test]: binding implies execution ([§1.5](#claim-rules)'s claim rules are thereby
    TEXT[!MUST,implementation,test]: instances of one predicate; [Property W5](#property-w5-claim-refinement)'s refinement claim
    TEXT[!MUST,implementation,test]: depends on this).
    TEXT[!MUST,implementation,test]: Producers MUST carry a unit test asserting reflexivity for every
    TEXT[!MUST,implementation,test]: constructed witness.
    TEXT[test]: Only reachable nodes contribute;
    TEXT[test]: nothing outside the reachable set may be included.
    TEXT[!MUST,implementation,test]: The closure MUST be computed at the finest granularity the
    TEXT[!MUST,implementation,test]: artifact demonstrably supports
    TEXT[!MUST,implementation,test]: (decisions.md, [Decision 17](decisions.md#decision-17) — deferral is legitimate only at the
    TEXT[!MUST,implementation,test]: artifact's ceiling or across a named architectural boundary);
    TEXT[!MUST,implementation,test]: what is normative now:
    TEXT[!MUST,implementation,test]: the closure MUST be a fixpoint (no truncation at a depth bound),
    TEXT[!MUST,implementation,test]: and the semantics is *consulted* (strength `Consulted`, [§1.3](#provenance)),
    TEXT[!MUST,implementation,test]: not load-bearing dependency.
    TEXT[!MUST,implementation,test]: **The closure ceiling MUST be stated per producer, because it
    TEXT[!MUST,implementation,test]: bounds what unit granularity buys.** Where a prover checks a
    TEXT[!MUST,implementation,test]: function's obligations in one solver query and assumes callee
    TEXT[!MUST,implementation,test]: contracts whole (Verus does both, §5.5), every discharge unit
    TEXT[!MUST,implementation,test]: inside a function carries the identical function-level consulted
    TEXT[!MUST,implementation,test]: closure: finer units buy precise identity and legible failures,
    TEXT[!MUST,implementation,test]: not smaller witnesses, and discharge verdicts within one function
    TEXT[!MUST,implementation,test]: do not differ across its units at Consulted strength.
    TEXT[!MUST,implementation,test]: Reports and documentation MUST NOT present clause-level units as
    TEXT[!MUST,implementation,test]: implying clause-level evidence.

  SECTION: [5.5 The Verus producer](#verus-producer)
    TEXT[!MUST,implementation,test]: A flat span inventory of one block covers essentially
    TEXT[!MUST,implementation,test]:   only its own extent, so the Verus producer MUST parse its
    TEXT[!MUST,implementation,test]:   artifact once into a structure of obligation nodes and
    TEXT[!MUST,implementation,test]:   reference edges and derive everything else from that
    TEXT[!MUST,implementation,test]:   structure — parse-into-structure is a MUST, not an
    TEXT[!MUST,implementation,test]:   optimization.
    TEXT[!MUST,implementation,test]: - The aggregate executability map MUST NOT be delivered as a
    TEXT[!MUST,implementation,test]:   witness: it is many obligations wearing one map, and delivering
    TEXT[!MUST,implementation,test]:   it would violate [§4.2](#obligation-individuation) by
    TEXT[!MUST,implementation,test]:   construction; it exists only as pass-1 scaffolding inside the
    TEXT[!MUST,implementation,test]:   producer.
    TEXT[!MUST,implementation,test]: - The Verus producer MUST treat `FunctionSst` blocks as closure
    TEXT[!MUST,implementation,test]:   nodes and `Fun :path` references as edges, and MUST support
    TEXT[!MUST,implementation,test]:   discharge units of all four kinds: obligation extents,
    TEXT[!MUST,implementation,test]:   `:enss` clause spans, `LoopInv` spans, and proof-assert spans
    TEXT[!MUST,implementation,test]:   (decisions.md, Decision 18).
    TEXT[implementation,test]: Unit labels (decisions.md, Decision 20): `ProofNoteLabel` text
    TEXT[implementation,test]:   when the artifact records it, otherwise span identity
    TEXT[implementation,test]:   (function path + unit kind + clause index).
    TEXT[!MUST,implementation,test]:   Until the upstream `proof_note`-on-ensures defect is fixed,
    TEXT[!MUST,implementation,test]:   ensures-clause labels MUST come from span identity.
