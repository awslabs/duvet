target = "design/witness/spec.md#witnessable-annotations"

# 5.1 Witnessable annotations
#
# Prover producers MUST construct witnesses for `type=test`
# annotations only
# (decisions.md, Decision 6;
# self-discharging implication annotations are a deferred separate
# feature).

[[spec]]
level = "MUST"
quote = '''
Prover producers MUST construct witnesses for `type=test`
annotations only
(decisions.md, Decision 6;
self-discharging implication annotations are a deferred separate
feature).
'''

